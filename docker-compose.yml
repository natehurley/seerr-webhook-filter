services:
  seerr-webhook:
    build: .
    container_name: seerr-webhook
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - JELLYFIN_URL=http://YOUR_SERVER/Streamyfin/notification
      - JELLYFIN_API_KEY=YOUR_API_KEY